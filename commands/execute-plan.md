---
description: "Legacy alias — ask for the executing-plans skill (批红)"
---

Tell your human partner that this command is a legacy alias. They should ask you to use the `executing-plans` skill (即本分叉里的"批红"，但 skill 名与上游一致，仍是 `executing-plans`).
